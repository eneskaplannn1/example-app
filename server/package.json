{
  "name": "plant-care-notification-server",
  "version": "1.0.0",
  "description": "Server-side notification service for plant care app",
  "main": "cronJob.ts",
  "scripts": {
    "start": "ts-node cronJob.ts",
    "build": "tsc",
    "dev": "ts-node-dev --respawn cronJob.ts",
    "test": "jest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
